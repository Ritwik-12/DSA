//given a linke list convert the it into int corresponding integer
#include<bits/stdc++.h>
using namespace std;
// linklist* reverse(linklist*head){

// }
int main()
{

    list<int> linklist;
    linklist.push_back(10);
    linklist.push_back(20);
    linklist.push_back(30);
    linklist* ptr=head;
    while(ptr!=NULL){
        cout<<"ptr->value"<<" ";
        ptr=ptr->next;
    }
}